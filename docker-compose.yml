version: "3.9"

services:
  kos-organism:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - ./.cache:/app/.cache
      - ./.cache/arc_agi:/app/.cache/arc_agi
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
