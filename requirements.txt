# KOS-AGI Organism v9.0 — Dependencies
# Python >= 3.11 required

# === Core Server ===
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0

# === Numerical Engine ===
numpy>=1.24.0

# === Rust Kernel (build from source) ===
# cd kos_rust && maturin develop --release
maturin>=1.0.0
