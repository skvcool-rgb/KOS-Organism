"""
KOS Phase 2 -- Typed Object-Centric Guided Synthesis

Phase 2 transforms the organism from blind evolutionary search into
structured, type-safe, object-aware program generation with:

    1. Type System      -- every op has a signature, invalid programs die pre-execution
    2. Perception       -- grids become objects with relations
    3. Object Graph     -- nodes + edges replace raw pixels
    4. Constraints      -- predict solution family before search
    5. Guided Generator -- typed beam search + macro-biased seeds
    6. Fitness          -- correctness + simplicity + reuse + generalization
    7. REM Compression  -- canonicalize + cluster + promote macros
    8. Promotion        -- recurring macros become permanent primitives
"""
